<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md3_dark_background">

    <!-- ========== 欢迎页 (首次启动) ========== -->
    <LinearLayout
        android:id="@+id/welcome_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="48dp"
        android:visibility="visible">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/logo"
            android:scaleType="fitCenter"
            android:contentDescription="ADOJAS logo" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="ADOJAS"
            android:textSize="48sp"
            android:textColor="@color/md3_dark_primary"
            android:textStyle="bold"
            android:letterSpacing="0.08"
            android:fontFamily="sans-serif-light" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="A Dance of Fire and Ice Simulator"
            android:textSize="16sp"
            android:textColor="@color/md3_dark_onSurface"
            android:alpha="0.7" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="36dp"
            android:text="欢迎来到 ADOJAS\n请先确认已导入谱面文件"
            android:textSize="14sp"
            android:textColor="@color/md3_dark_onSurface"
            android:alpha="0.5"
            android:gravity="center"
            android:lineSpacingExtra="6dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_start"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_marginTop="48dp"
            android:text="@string/enter_game"
            android:textSize="16sp"
            style="@style/Widget.Material3.Button.Tonal" />

    </LinearLayout>

    <!-- ========== WebView 容器 ========== -->
    <FrameLayout
        android:id="@+id/webview_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

</FrameLayout>
